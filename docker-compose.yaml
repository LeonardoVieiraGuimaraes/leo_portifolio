#version: "3.9"
services:
  portifolio:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 80
    ports:
      8080:80

#    volumes:
#      - /root/mysite/mysite:/usr/share/nginx/html

    networks:
      - nginx-network

networks:
  nginx-network:
    external: true