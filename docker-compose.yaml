#version: "3.9"
services:
  portifolio:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 91
#    ports:
#      - 801:801

#    volumes:
#      - /root/mysite/mysite:/usr/share/nginx/html

    networks:
      - nginx-network

networks:
  nginx-network:
    external: true