#version: "3.9"
networks:
  nginx_network:
    # driver: bridge
    external: true

# networks:
#   nginx-network:
#     external: true
#     # driver: bridge

services:
  portifolio:
    build:
      context: .
      dockerfile: Dockerfile
    # expose:
    #   - 8001
    # ports:
    #  - 8001:8001

    volumes:
      - ./dist:/usr/share/nginx/html

    networks:
      - nginx_nginx_net
      # - nginx-network

